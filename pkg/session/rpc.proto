syntax = "proto3";
package session;

service AppGate {
    rpc OpenApp(stream OpenAppRequest) returns(stream AppResponse) {}
}

message App {
    // optional
    string name = 1;
    string image = 2;
    repeated string hostpath = 3;
}

message OpenAppRequest {
    App App = 1;
    repeated string Stdin = 2;
}

message AppResponse {
    string Stdout = 1;
    string Stderr = 2;
}
